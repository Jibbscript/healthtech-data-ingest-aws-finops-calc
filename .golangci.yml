run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - revive
    - staticcheck
    - gocyclo
issues:
  exclude-rules:
    - path: _test\.go
      linters: [gocyclo]
